import 'package:flutter/material.dart';

class MessageGroupScreen extends StatelessWidget {
  const MessageGroupScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
