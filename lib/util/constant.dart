import 'package:flutter/material.dart';

const String kSharedTokenKey = 'BearerToken';
const Color kPrimaryColor = Color(0xFF333333);
const double kDefaultBorderRadius = 10.0;

class PaddingConstant {
  static const double quarterPadding = defaultPadding / 4;
  static const double halfPadding = defaultPadding / 2;
  static const double defaultPadding = 16.0;
  static const double doublePadding = defaultPadding * 2;
}

class ColorConstant {
  static List<MaterialAccentColor> messageColorList = Colors.accents;
}
